# Doctrine

## Core

```text
brain owns state
no duplicated consciousness
organs are not separate minds
objective maturity is identity center
communication is motor output
language/math is unit-total meaning field logic
memory is belief/context
truth is separate from belief
self-improvement is internal plasticity
no final truth promotion yet
```

## Epistemic Octahedron invariant

```text
active state: |x| + |y| + |z| = 1
null origin: {x:0,y:0,z:0}
collapse: {x:0,y:-1,z:0}
objective maturity peak: {x:0,y:1,z:0}
```

The peak is an identity center, not a dogma engine.

## Language/math unit-total rule

```text
meaning(term) = 1
sum(abs(dimension.weight)) = 1
```

Growth happens by subdivision, relation, contradiction handling, and consistency, not keyword routing.
